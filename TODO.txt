
* fix sprite rotation
* invert v on sprites
* log errors to separate file
* set collider radius to sprite max( width, height ) by default
* handle errors in shader creation
* remove duplicated code in managers
* get and set things in batches
* use chrono lib
* update api to include expected errors
* create animation component using a rect per frame
* implement camera
* destroy entities
* create and destroy entities at random for test
* use matrices for projections
* controller movement
* rendering layers
* sorting in layers
* n² collision detection
* space-partitiones collision detection
* implement obb colliders
* allow multiple colliders per entity
* use cppcheck
